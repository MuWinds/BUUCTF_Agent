# 示例项目

这是 fake-llm 的沙箱工作区，供功能测试使用。

内容是固定的：自动化测试会把整个目录复制到临时目录再跑，
手动测试时把应用的工作区指向这里即可。改动它会让断言失效。

## 结构

- `src/app.ts` —— 入口，含一处刻意留下的待修改字符串
- `src/util.ts` —— 工具函数
- `docs/notes.md` —— 含可被 Grep 命中的关键词
